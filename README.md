
  # Start project

  This is a code bundle for Start project. The original project is available at https://www.figma.com/design/Oeb3nsKF47Xcb7fXg4yzqW/Start-project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  